[project]
name = "tasks3"
version = "0.1.0"
description = "Tiny JSON task manager with tests"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
tasks3 = "tasks3.__init__:main"
