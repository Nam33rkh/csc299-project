# Specification: CLI Tasks Manager

## Overview
A command-line tasks manager that supports:
- adding tasks
- listing tasks
- searching tasks
- marking tasks as complete
- deleting tasks

Tasks are stored in a local JSON file.  
The CLI must run cross-platform (macOS, Windows, Linux).

This project is built using GitHub’s spec-kit.
